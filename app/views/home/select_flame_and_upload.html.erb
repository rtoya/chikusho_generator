<div class="container">
  <div class="page-header">
    <h1>フレームの選択・写真のアップロード&nbsp;<small>(Step 1/3)</small></h1>
  </div>
  <p>使用するフレームを選択してください。</p>

  <div class="row">
    <div class="col-md-6">
      <% CHIKUSHO_FLAMES.each_with_index do |flame_img, index| %>
        <div class="col-md-4 thumb_col">
          <%= link_to '#', id: "flame#{index}", class: "thumbnail flame_thumb #{'photo_bg_blk' if index % 3 == 1}" do %>
            <%= image_tag "#{flame_img}" %>
          <% end %>
        </div>
      <% end %>
    </div>

    <%= form_for @image, :url => { :controller => 'images', :action => 'upload'} do |f| %>
      <%= f.file_field :input %>
      <%= f.check_box :public_flg %>
      <%= f.submit %>
    <% end %>

    <div class="col-md-6">
      <div class="col-md-3 thumb_col">
        <%= link_to '次に進む', adjust_photo_path, class: 'btn btn-success btn-lg next_btn', disabled: 'disabled' %>
      </div>
    </div>
  </div>
</div>
