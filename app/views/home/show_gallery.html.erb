<div class="container">
  <div class="jumbotron">
    <div class="page-header">
      <h1>ちくしょうギャラリーを見る</h1>
    </div>
    <p>今までに公開された「ちくしょう写真」のギャラリーです。</p>

    <div class="row">
        <% @images.each do |image| %>
        <div class="col-md-4 chiku_thumb">
          <%= link_to show_photo_path(image.random_hash), class: 'thumbnail' do %>
            <%= image_tag(get_output_path(image.random_hash), height: '150px') %>
          <% end %>
        </div>
      <% end %>
    </div>
    <div><%= paginate @images %></div>
  </div>
</div>
