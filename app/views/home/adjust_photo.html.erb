<div class="container">
  <div class="page-header">
    <h1>画像位置の調整&nbsp;<small>(Step 2/3)</small></h1>
  </div>
  <p>
    写真をアップロードしてください。アップロードした写真のサイズ・位置を調整できます。<br>
    左の画像上でドラッグすることで、表示したい領域を調整します。
  </p>

  <div class="row photo_adjust_area">
    <div class="col-md-6 crop_area">
      <h3>表示領域の調整</h3>
      <%= image_tag @image.input.url, width: '480px', height: 'auto', id: 'jcrop_target' %>
    </div>
    <div class="col-md-6 preview_area">
      <h3>プレビュー</h3>
      <div class="preview_photo">
        <%= image_tag @image.input.url, class: 'preview' %>
      </div>
      <%= image_tag CHIKUSHO_FLAMES[params[:fid].to_i], width: '420px', style: 'position:relative;' %>
    </div>
  </div>

  <%= form_tag create_photo_path, id: 'create_photo' do %>
    <%= hidden_field_tag :coords_w %>
    <%= hidden_field_tag :coords_h %>
    <%= hidden_field_tag :coords_x %>
    <%= hidden_field_tag :coords_y %>
    <%= hidden_field_tag :photo_id, @image.id %>
    <%= hidden_field_tag :flame_id, params[:fid] %>

    <p align="center"><%= submit_tag '画像を作成する', class: 'btn btn-success btn-lg', disabled: 'disabled' %></p>
  <% end %>
</div>
